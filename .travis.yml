language: node_js
node_js:
  - "iojs"
  - "7"
services:
  - postgresql
env:
  - NODE_ENV=test;DB_HOST="localhost";DB_PORT="5432";DB_USER="postgres";DB_PASSWORD="hisgrace";DB_DATABASE="stackoverflow-lite-test"
before_script:
  - psql -c 'create database stackoverflow-lite-test;' -U postgres